server:
  port: 8081
spring:
  application:
    name: feign-server-a
  main:
    allow-bean-definition-overriding: true
# 注册中心eureka配置
eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8761/eureka
feign:
  hystrix:
    enabled: true