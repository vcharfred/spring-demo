eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/
spring:
  application:
    name: config-service
  cloud:
    config:
      discovery:
        service-id: CONFIG-SERVER
        enabled: true
      profile: rel
      label: master